# Ignore everything
*

# But not the .gitignore itself
!.gitignore

# Allow the openclaw directory (and everything inside it)
!openclaw/
!openclaw/**

# Still ignore system files everywhere
.DS_Store
